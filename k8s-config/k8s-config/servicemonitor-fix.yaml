# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   name: local-target-app-servicemonitor
#   namespace: monitoring
#   labels:
#     release: monitoring
# spec:
#   selector:
#     matchLabels:
#       app: local-target-app
#   namespaceSelector:
#     matchNames:
#     - default
#   endpoints:
#   - targetPort: 8080  # Use targetPort instead of port
#     path: /metrics
#     interval: 30s